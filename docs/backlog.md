# Backlog

- Move any remaining retired CLI implementation details into business modules and keep parser registration restricted to the public surface.
- Remove compatibility wrappers only after the existing non-CLI callers have been migrated.
